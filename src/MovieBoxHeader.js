import React from 'react';

const MovieBoxHeader = (props) => (
    <div className ="category-header">
        <h1>
            {props.header}
        </h1>
    </div>
)

export default MovieBoxHeader;